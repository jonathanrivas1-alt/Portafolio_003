/**
 * GrainOverlay — ruido cinematográfico encima de todo el viewport.
 * Server component: sin JS, solo CSS animado.
 */
export function GrainOverlay() {
  return <div className="grain-overlay" aria-hidden />;
}
